# terminal menu

menu "terminal"

config TERMINAL_APP
	bool "Build terminal application"
	default y

if TERMINAL_APP

config TERMINAL_POWER_UP
	bool "Running when power up"
	default n

endif

endmenu
