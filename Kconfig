comment "This is using to configure application"

# application
config APPLICATION1
	tristate "Add application1 to project"
	default y

config APPLICATION2
	tristate "Add application2 to project"
	default y

config APPLICATION3
	depends on GUI
	tristate "Add application3 to project"
	default n

config APPLICATION4
	tristate "Add application4 to project"
	default m

config APPLICATION5
	tristate "Add application5 to project"
	default m

config APPLICATION6
	tristate "Add application6 to project"
	default m


if GENERATED_APP

config TAICHI
	bool "Add taichi to project"
	default y

config TERMINAL
	bool "Add terminal to project"
	default y

config LAUNCH
	bool "Add launch to project"
	default y

config SETTINGS
	bool "Add settings to project"
	default y
	
endif
